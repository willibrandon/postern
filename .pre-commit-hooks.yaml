- id: postern
  name: postern check
  description: Checks postgresql.conf, postgresql.auto.conf, pg_hba.conf and pg_ident.conf the way the PostgreSQL server reads them.
  entry: scripts/pre-commit.sh
  language: script
  files: (^|/)(postgresql(\.auto)?\.conf|pg_hba\.conf|pg_ident\.conf)$
